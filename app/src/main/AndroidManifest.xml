<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.afinal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final">
        <activity android:name=".instagram.GalleryActivity"></activity>
        <activity android:name=".instagram.CameraActivity" />
        <activity android:name=".instagram.AddArticleActivity" />
        <activity android:name=".evaluate.EvaluateBoardInsideActivity" />
        <activity android:name=".evaluate.EvaluateBoardModifyActivity" />
        <activity android:name=".evaluate.EvaluateBoardWrite" />
        <activity android:name=".evaluate.EvaluateBoard" />
        <activity android:name=".board.FreeBoardModifyActivity" />
        <activity android:name=".board.FreeBoardInsideActivity" />
        <activity android:name=".board.FreeBoardActivity" />
        <activity android:name=".board.BoardWriteActivity" />
        <activity android:name=".googleLogin" />
        <activity android:name=".MainPageActivity" />
        <activity android:name=".JoinActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>